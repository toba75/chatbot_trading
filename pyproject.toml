[project]
name = "chatbot-trading"
version = "0.4.0"
description = "Pipeline documentaire minimal fondé sur Docling"
requires-python = "==3.12.8"
dependencies = [
    "docling-core==2.87.1",
    "fastapi>=0.141.1",
    "fonttools==4.60.1",
    "latex2mathml==3.81.0",
    "pymupdf==1.27.2.2",
    "pypdf==6.10.2",
    "python-multipart>=0.0.32",
    "uvicorn>=0.52.0",
]

[project.scripts]
pdf-math-audit = "pdf_math_audit.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "httpx2>=2.9.1",
    "pillow==12.3.0",
    "pytest==8.4.2",
    "pytest-xdist==3.8.0",
    "reportlab==4.4.9",
    "ruff==0.15.4",
]

[tool.hatch.build.targets.wheel]
packages = ["pdf_math_audit"]

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["live: nécessite le conteneur Docling CUDA réellement démarré"]
