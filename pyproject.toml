[project]
name = "chatbot-trading"
version = "0.1.0"
description = "Pipeline documentaire minimal fondé sur Docling"
requires-python = "==3.12.8"
dependencies = []

[dependency-groups]
dev = [
    "docling-core==2.87.1",
    "pytest==8.4.2",
    "pytest-xdist==3.8.0",
    "ruff==0.15.4",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["live: nécessite le conteneur Docling CUDA réellement démarré"]
