# Journal M-006

## Clôture T-001

- Scénario BDD: Given M-004 et M-005 sont présents dans `master`; When les gates de précondition M-006 sont exécutées; Then M-006 ne peut commencer que si les validations, la traçabilité, les ADR, les frontières d'architecture et les preuves M-005 sont GREEN.
- Commit RED: `719977eef41061d13c0b430c5c03bcd6ab40619b` (`test(m006): couvrir la precondition green des claims`).
- Commit GREEN: `test(m006): etablir la precondition green des claims`.
- Implémentation: `scripts/validate_m006_precondition.ps1` vérifie la branche autorisée, l'alignement `origin/master`, la présence des artefacts M-004/M-005 dans `master`, l'exécution conclue de `scripts/test.ps1` et `scripts/lint.ps1`, puis publie `docs/governance/m006_precondition_green.md`.
- Garde-fous livrés: timeout de gate explicite et non silencieux; rapport hors dépôt refusé; statut GREEN impossible sans preuve de commande; branche M-006 autorisée sans accepter une branche amont non fusionnée comme preuve; corrections de récursion M-003/M-004 pour éviter que les préconditions imbriquées relancent indéfiniment la gate globale.
- Traçabilité et gates: `scripts/test.ps1` enrôle les tests `tests/m006/validate_m006_precondition_acceptance.ps1` et `tests/m006/validate_m006_precondition_unit.ps1`; les préconditions M-003, M-004, M-005 et M-006 disposent chacune d'une garde d'exécution imbriquée.
- ADR: non requise; T-001 applique ADR-010 et les décisions existantes de gouvernance des gates sans changer leur sens.
- Validations GREEN: `tests/m003/validate_m003_precondition_unit.ps1`; `tests/m004/validate_m004_precondition_unit.ps1`; `tests/m005/validate_m005_precondition_unit.ps1`; `tests/m006/validate_m006_precondition_unit.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `tests/m004/validate_m004_precondition_acceptance.ps1`; `tests/m005/validate_m005_precondition_acceptance.ps1`; `tests/m006/validate_m006_precondition_acceptance.ps1`; `scripts/validate_m006_precondition.ps1 -Path .\docs\governance\m006_precondition_green.md` (`2 gate(s), 7 artefact(s) amont vérifié(s)`); `git diff --check`; `scripts/lint.ps1` (`14 validation(s), 0 test(s)`); `scripts/test.ps1` (`14 validation(s), 116 test(s)`).
- Risques résiduels: la gate globale reste longue parce qu'elle réexécute les préconditions amont; aucun risque fonctionnel bloquant identifié pour démarrer T-002.

## Clôture T-002

- Scénario BDD: Given des preuves candidates KA avec `SourceLocator` résolvable; When la spécification M-006 est publiée; Then chaque comportement de claim nomme son invariant, son scénario BDD, son test RED, ses ADR applicables et sa commande de validation.
- Commit RED: `9a04d2b` (`test(m006): couvrir la specification des claims verifiables`).
- RED T-002 confirmé: `tests/m006/validate_m006_specification_acceptance.ps1` et `tests/m006/validate_m006_specification_unit.ps1` échouaient tant que `scripts/validate_m006_specification.ps1` et `docs/specs/m006_claims_verifiables.md` étaient absents.
- Implémentation: `docs/specs/m006_claims_verifiables.md` publie la mission EG, les agrégats `Claim`, `VerificationCase` et `DependencyGroup`, les objets-valeur, politiques, états, ports, événements EG, API publiques, erreurs, métriques, exclusions et comportements exécutables EG-001 à EG-009.
- Garde-fous publiés: `VERIFIED` exige une preuve directe admissible; la portée du claim ne dépasse pas la portée commune des preuves; le LLM propose et la politique décide; aucun claim EG n'est stocké dans l'index documentaire; EG consomme `KnowledgeSearchPort` sans accès direct à Qdrant; aucun score n'est traité comme verdict métier.
- Traçabilité et gates: `scripts/validate_m006_specification.ps1` est enrôlé dans `scripts/lint.ps1` et `scripts/test.ps1`; `REQ-M006-001` et `REQ-M006-002` sont présents dans `docs/traceability/matrix.md`; les compteurs de gates impactés sont alignés sur `15 validation(s)`, `118 test(s)` en gate globale et `112 test(s)` en précondition M-003 imbriquée.
- ADR: non requise; T-002 applique ADR-006, ADR-010, DDD-ADR-003, DDD-ADR-005, DDD-ADR-007 et DDD-ADR-010 sans changer leur sens.
- Validations GREEN: `tests/m006/validate_m006_specification_acceptance.ps1`; `tests/m006/validate_m006_specification_unit.ps1`; `scripts/validate_m006_specification.ps1`; `scripts/validate_traceability.ps1` (`65 exigence(s)`); `tests/m004/validate_m004_traceability_acceptance.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `scripts/lint.ps1` (`15 validation(s), 0 test(s)`); `scripts/test.ps1` (`15 validation(s), 118 test(s)`).
- Risques résiduels: aucun risque résiduel identifié pour la publication de spécification; l'implémentation applicative des claims reste portée par T-003 à T-009, puis reliée aux métriques finales par T-010.

## Clôture T-003

- Scénario BDD: Given un passage source publié contient deux conclusions et une limitation explicite; When EG extrait les claims candidats; Then deux claims `DRAFT` atomiques sont créés avec leur portée, leur limitation et leur span de preuve sans statut `VERIFIED`.
- Commit RED: `de7d560` (`test(m006): couvrir extraction claims atomiques`).
- RED T-003 confirmé: `tests/m006/validate_claim_extraction_acceptance.ps1` et `tests/m006/validate_claim_extraction_unit.ps1` échouaient sur `ModuleNotFoundError: app.evidence_governance.adapters.deterministic_claim_extractor`, car le port, le double et le handler EG n'existaient pas encore.
- Implémentation: `app/evidence_governance/domain/claim_extraction.py` ajoute `CanonicalProposition`, `ClaimScope`, `ClaimCondition`, `Limitation`, `EvidenceSpan`, `ClaimExtractionProposal`, `DraftClaim`, `ClaimDrafted`, `ClaimAtomicityPolicy` et `ClaimCanonicalizationPolicy`; `app/evidence_governance/application/extract_claims.py` ajoute le port `ClaimExtractor`, la commande et le handler `ExtractClaimsFromEvidenceHandler`; les adaptateurs mémoire livrent `DeterministicClaimExtractor` et `InMemoryClaimDraftRepository`.
- Garde-fous livrés: aucun claim extrait ne devient `VERIFIED`; aucun `VerifiedClaimRef` ni `verification_id` n'est produit par l'extraction; le span source et son `SourceLocator` sont obligatoires; les champs structurés absents sont refusés; l'atomicité, la modalité, la négation et les conditions sont contrôlées par politiques explicites.
- Traçabilité et gates: `tests/m006/validate_claim_extraction_acceptance.ps1` et `tests/m006/validate_claim_extraction_unit.ps1` sont enrôlés dans `scripts/test.ps1`; `REQ-M006-003` est ajouté à `docs/traceability/matrix.md`; la gate globale attend désormais `15 validation(s), 120 test(s)`.
- ADR: non requise; T-003 applique ADR-006, DDD-ADR-005 et DDD-ADR-007 sans changer leur sens.
- Validations GREEN: `tests/m006/validate_claim_extraction_acceptance.ps1`; `tests/m006/validate_claim_extraction_unit.ps1`; `scripts/validate_traceability.ps1` (`66 exigence(s)`); `scripts/validate_architecture_boundaries.ps1`; `scripts/lint.ps1` (`15 validation(s), 0 test(s)`); `scripts/test.ps1` (`15 validation(s), 120 test(s)`); `git diff --check`.
- Risques résiduels: le double d'extraction est déterministe et local pour T-003; aucun fournisseur LLM externe n'est autorisé par cette tâche.

## Clôture T-004

- Scénario BDD: Given un claim `DRAFT` et une preuve candidate dont le `SourceLocator` pointe vers une version canonique publiée; When la preuve `SUPPORTS_DIRECTLY` est attachée; Then le claim passe à `EVIDENCE_ATTACHED` et conserve le `EvidenceRef` complet avec son hash de span.
- Commit RED: `0668268` (`test(m006): couvrir attachement preuve claim`).
- RED T-004 confirmé: `tests/m006/validate_claim_evidence_attachment_acceptance.ps1` et `tests/m006/validate_claim_evidence_attachment_unit.ps1` échouaient sur `ModuleNotFoundError: app.evidence_governance.adapters.in_memory_canonical_evidence_reader`, car le port, le modèle d'attachement et les doubles mémoire n'existaient pas encore.
- Implémentation: `app/evidence_governance/domain/claim_evidence.py` ajoute `Claim`, `ClaimStatus`, `CanonicalEvidenceSpan`, `EvidenceAssociation`, `EvidenceAdmissibilityPolicy` et `EvidenceAttachedToClaim`; `app/evidence_governance/application/attach_evidence.py` ajoute `CanonicalEvidenceReader`, `ClaimRepository`, `AttachEvidenceToClaimCommand` et `AttachEvidenceToClaimHandler`; les adaptateurs mémoire livrent `InMemoryCanonicalEvidenceReader` et `InMemoryClaimRepository`.
- Garde-fous livrés: aucune preuve sans `SourceLocator`; aucune relation par défaut; seule la relation `SUPPORTS_DIRECTLY` est admissible; aucun fallback vers une page ou un item voisin; refus de hash absent ou incohérent; refus de localisateur non résolvable; refus des versions retirées ou en quarantaine via le contrat `SourceLocator`; refus de doublon d'évidence; refus de transition depuis `UNDER_VERIFICATION`.
- Traçabilité et gates: `tests/m006/validate_claim_evidence_attachment_acceptance.ps1` et `tests/m006/validate_claim_evidence_attachment_unit.ps1` sont enrôlés dans `scripts/test.ps1`; `REQ-M006-004` est ajouté à `docs/traceability/matrix.md`; la gate globale attend désormais `15 validation(s), 122 test(s)` et la précondition M-003 imbriquée attend `116 test(s)` après exclusions de récursion.
- ADR: non requise; T-004 applique DDD-ADR-003 et DDD-ADR-005 sans changer leur sens.
- Validations GREEN: `tests/m006/validate_claim_evidence_attachment_acceptance.ps1`; `tests/m006/validate_claim_evidence_attachment_unit.ps1`; `scripts/validate_traceability.ps1` (`67 exigence(s)`); `tests/m004/validate_m004_traceability_acceptance.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `scripts/lint.ps1` (`15 validation(s), 0 test(s)`); `scripts/test.ps1` (`15 validation(s), 122 test(s)`); `git diff --check`.
- Risques résiduels: le repository mémoire et le lecteur canonique mémoire sont strictement des doubles locaux pour T-004; aucune persistance durable ni API publique d'attachement n'est introduite par cette tâche.

## Clôture T-005

- Scénario BDD: Given une affirmation `EVIDENCE_ATTACHED`; When elle est soumise à vérification indépendante puis qu'aucune preuve directe admissible ou aucune portée conservée n'autorise `VERIFIED`; Then `ClaimSubmittedForVerification` et `VerificationDecisionRecorded` sont enregistrés, le claim passe par `UNDER_VERIFICATION`, puis devient `VERIFIED` avec `VerifiedClaimRef` contrôlé ou `REJECTED` avec raisons explicites.
- Commit RED: `9fbe20b` (`test(m006): couvrir verification claim preuve directe`).
- RED T-005 confirmé: `tests/m006/validate_claim_verification_acceptance.ps1` et `tests/m006/validate_claim_verification_unit.ps1` échouaient sur `ModuleNotFoundError: app.evidence_governance.adapters.in_memory_verification_case_repository`, car le cas de vérification, le port de vérificateur et le handler EG n'existaient pas encore.
- Implémentation: `app/evidence_governance/domain/claim_verification.py` ajoute `ClaimSubmittedForVerification`, `VerificationCase`, `VerificationDecisionRecorded`, `ClaimVerificationPolicy`, `ScopePreservationPolicy`, `IndependentVerificationReport`, `VerificationVerdict`, `ReasonCode`, `ClaimVerified` et `ClaimRejected`; `app/evidence_governance/application/verify_claim.py` ajoute `SubmitClaimForVerification`, le port `IndependentClaimVerifier`, `VerificationCaseRepository` et `VerifyClaimHandler`; `app/evidence_governance/adapters/in_memory_verification_case_repository.py` livre un repository mémoire strict pour les cas immuables.
- Garde-fous livrés: aucun score ne devient verdict métier; aucun verdict par défaut; `DRAFT -> UNDER_VERIFICATION` est interdit; `VerifiedClaimRef` n'est publié qu'après preuve directe `SUPPORTS_DIRECTLY`, portée conservée, verdict `ENTAILED` et métadonnées modèle/prompt/politique; `PARTIALLY_ENTAILED` avec portée élargie est rejeté par `CLAIM_SCOPE_EXCEEDS_EVIDENCE`; un rejet reste explicite et non rouvrable pour la même version.
- Traçabilité et gates: `tests/m006/validate_claim_verification_acceptance.ps1` et `tests/m006/validate_claim_verification_unit.ps1` sont enrôlés dans `scripts/test.ps1`; `REQ-M006-005` est ajouté à `docs/traceability/matrix.md`; la gate globale attend désormais `15 validation(s), 124 test(s)` et la précondition M-003 imbriquée attend `118 test(s)` après exclusions de récursion.
- ADR: non requise; T-005 applique ADR-006, DDD-ADR-003, DDD-ADR-005 et DDD-ADR-007 sans changer leur sens.
- Validations GREEN: `tests/m006/validate_claim_verification_acceptance.ps1`; `tests/m006/validate_claim_verification_unit.ps1`; `scripts/validate_traceability.ps1` (`68 exigence(s)`); `scripts/lint.ps1` (`15 validation(s), 0 test(s)`); `scripts/test.ps1` (`15 validation(s), 124 test(s)`); `git diff --check`.
- Risques résiduels: le vérificateur indépendant est seulement un port et le repository mémoire est un double local pour T-005; aucune persistance durable ni API HTTP de vérification n'est introduite par cette tâche.

## Clôture T-006

- Scénario BDD: Given trois documents rattachés au même `DependencyGroup`; When le nombre de confirmations indépendantes est calculé; Then une seule confirmation indépendante est comptabilisée.
- Commit RED: `2961e85` (`test(m006): couvrir confirmations independantes`).
- RED T-006 confirmé: `tests/m006/validate_dependency_group_acceptance.ps1` et `tests/m006/validate_dependency_group_unit.ps1` échouaient sur `ModuleNotFoundError: app.evidence_governance.adapters.in_memory_dependency_group_repository`, car l'agrégat, le repository, le handler et le compteur EG n'existaient pas encore.
- Implémentation: `app/evidence_governance/domain/dependency_group.py` ajoute `DependencyGroup`, `ClaimDependencyAssignment`, `ClaimDependencyAssigned`, `SourceIndependencePolicy` et `IndependentSupportCount`; `app/evidence_governance/application/dependency_groups.py` ajoute la commande `AssignClaimDependencyGroup`, son handler et la requête `CountIndependentSupport`; `app/evidence_governance/adapters/in_memory_dependency_group_repository.py` livre un repository mémoire strict.
- Garde-fous livrés: aucun groupe par défaut; aucune affectation par titre proche ou heuristique silencieuse; tout document accepté sans groupe est refusé; tout doublon d'affectation est refusé; un claim `VERIFIED` et tout groupe déjà utilisé par un claim vérifié sont non modifiables; le repository n'expose aucune suppression et refuse les sauvegardes qui retirent des affectations.
- Traçabilité et gates: `tests/m006/validate_dependency_group_acceptance.ps1` et `tests/m006/validate_dependency_group_unit.ps1` sont enrôlés dans `scripts/test.ps1`; `REQ-M006-006` est ajouté à `docs/traceability/matrix.md`; la gate globale attend désormais `15 validation(s), 126 test(s)` et la précondition M-003 imbriquée attend `120 test(s)` après exclusions de récursion.
- ADR: non requise; T-006 applique ADR-006, DDD-ADR-005 et DDD-ADR-010 sans changer leur sens.
- Validations GREEN: `tests/m006/validate_dependency_group_acceptance.ps1`; `tests/m006/validate_dependency_group_unit.ps1`; `scripts/validate_traceability.ps1` (`69 exigence(s)`); `scripts/lint.ps1` (`15 validation(s), 0 test(s)`); `scripts/test.ps1` (`15 validation(s), 126 test(s)`); `git diff --check`.
- Risques résiduels: le repository mémoire reste un double local pour T-006; aucune persistance durable ni moteur graphe n'est introduit par cette tâche.
