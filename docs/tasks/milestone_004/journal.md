# Journal M-004 - Version canonique publiée

## Statut initial

- Planification créée depuis `docs/specs/plan_implementation_milestones_workstreams.md`.
- Dépendance directe: M-003.
- Milestones amont vérifiés dans `master`: M-000, M-001, M-002 et M-003.
- État initial des gates: `lint` GREEN; `test` RED sur `tests/m003/validate_m003_precondition_acceptance.ps1`.
- Cause RED conservée: `scripts/validate_m003_precondition.ps1` attendait `codex/milestone-m003-source-routee` alors que la base courante est `master` ou la branche M-004.

## Ordre d'exécution prévu

1. T-001 - Vérifier et rétablir la précondition GREEN M-004.
2. T-002 - Publier la spécification de version canonique.
3. T-003 - Convertir les pages selon la route explicite.
4. T-004 - Adjuger l'autorité textuelle par page.
5. T-005 - Contrôler la qualité de la version canonique.
6. T-006 - Publier une version canonique immuable.
7. T-007 - Rendre les SourceLocator résolvables.
8. T-008 - Publier l'événement CanonicalSourcePublished.
9. T-009 - Exposer la commande de conversion documentaire.
10. T-010 - Relier M-004 à la traçabilité et aux gates.

## Suivi d'exécution

| Tâche | Commit RED | Commit GREEN | ADR consultées | ADR créée ou modifiée | Validations GREEN déclarées |
|---|---|---|---|---|---|
| T-001 - Vérifier et rétablir la précondition GREEN M-004 | `b5036c60d07de6b8bdd4e8d27661fa4f78dab976` | `7be67660e5328cd8530a04bc33131a652b185daf` (`test(m004): retablir la precondition green avant version canonique`) | ADR-010 | Aucune | `tests/m003/validate_m003_precondition_unit.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `tests/m004/validate_m004_precondition_unit.ps1`; `tests/m004/validate_m004_precondition_acceptance.ps1`; `scripts/validate_m004_precondition.ps1 -Path .\docs\governance\m004_precondition_green.md`; `scripts/test.ps1`; `scripts/lint.ps1` |
| T-002 - Publier la spécification de version canonique | `826837c1e12b48acbdbc761cc0284ca35d6cf51a` | `87d6ba16ecdcfd71011b5281787c359735e8ce62` (`docs(m004): publier la specification de version canonique`) | ADR-001; ADR-002; ADR-003; ADR-004; DDD-ADR-003 | Aucune | `tests/m004/validate_m004_specification_acceptance.ps1`; `tests/m004/validate_m004_specification_unit.ps1`; `scripts/validate_m004_specification.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1` |
| T-003 - Convertir les pages selon la route explicite | `8ba1755` | `d594660e96d416ecc9c90dd1b83ef43810a4161d` (`feat(m004): convertir et fusionner les pages routees`) | ADR-001; ADR-002; ADR-003; ADR-004; DDD-ADR-003 | Aucune | `tests/m004/validate_page_conversion_acceptance.ps1`; `tests/m004/validate_page_conversion_unit.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1` |
| T-004 - Adjuger l'autorité textuelle par page | `c5208a6` | `4bcf57045c86517a199b43d3cf3c113837ba8c04` (`feat(m004): adjuger l autorite textuelle par page`) | ADR-004 | Aucune | `tests/m004/validate_text_authority_acceptance.ps1`; `tests/m004/validate_text_authority_unit.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1` |
| T-005 - Contrôler la qualité de la version canonique | `500af18` | `9d14598be58b1ea2ae225e7ed6a3a3e68e1c73a6` (`feat(m004): controler la qualite de version canonique`) | ADR-001; ADR-002; ADR-003; ADR-004 | Aucune | `tests/m004/validate_canonical_quality_acceptance.ps1`; `tests/m004/validate_canonical_quality_unit.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `scripts/validate_traceability.ps1`; `scripts/validate_architecture_boundaries.ps1`; `scripts/test.ps1`; `scripts/lint.ps1` |
| T-006 - Publier une version canonique immuable | `683b6dc` | `3cae8478efee0b80044d9c5e72912cb011830496` (`feat(m004): publier une version canonique immuable`) | ADR-001; DDD-ADR-003 | Aucune | `tests/m004/validate_canonical_publication_acceptance.ps1`; `tests/m004/validate_canonical_publication_unit.ps1`; `tests/m001/validate_source_contracts_acceptance.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1` |
| T-007 - Rendre les SourceLocator résolvables | `adaaf93` | `42d3221c0c86ab87128b29a3d0a3a40913d5a97c` (`feat(m004): rendre les source locator resolvables`) | DDD-ADR-003 | Aucune | `tests/m004/validate_source_locator_resolution_acceptance.ps1`; `tests/m004/validate_source_locator_resolution_unit.ps1`; `tests/m001/validate_source_locator_unit.ps1`; `scripts/validate_architecture_boundaries.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1` |
| T-008 - Publier l'événement CanonicalSourcePublished | `6e80443` | `62d1248d2fbc201cc99dcc015580cd489f549cfb` (`feat(m004): publier l evenement canonical source published`) | ADR-001; DDD-ADR-003; DDD-ADR-006; DDD-ADR-008 | Aucune | `tests/m004/validate_canonical_publication_event_acceptance.ps1`; `tests/m004/validate_canonical_publication_event_unit.ps1`; `tests/m001/validate_event_envelope_acceptance.ps1`; `tests/m002/validate_outbox_acceptance.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1` |
| T-009 - Exposer la commande de conversion documentaire | `85191e6` | `20f65cf6b9ea98832be8ab16c30c1a633d8b666a` (`feat(m004): exposer la commande de conversion documentaire`) | ADR-010; DDD-ADR-003; DDD-ADR-006; DDD-ADR-008 | Aucune | `tests/m004/validate_document_conversion_command_acceptance.ps1`; `tests/m004/validate_document_conversion_command_unit.ps1`; `tests/m003/validate_document_http_contract_acceptance.ps1`; `scripts/validate_architecture_boundaries.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1` |
| T-010 - Relier M-004 à la traçabilité et aux gates | `b32fe05` | `45839de20b30d629ab9ac6ccd9345a1f940fac8b` (`docs(m004): relier m004 aux gates et a la tracabilite`) | ADR-001; ADR-004; ADR-010; DDD-ADR-003; DDD-ADR-006; DDD-ADR-008 | Aucune | `tests/m004/validate_m004_traceability_acceptance.ps1`; `tests/m004/validate_m004_traceability_unit.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1` |

## Clôture T-001

- Scénario BDD: Given M-000, M-001, M-002 et M-003 sont présents dans `master`; When les gates de précondition M-004 sont exécutées depuis la base courante; Then M-004 ne commence que si `test`, `lint`, la traçabilité, les ADR, les frontières d'architecture et la preuve M-003 post-merge sont GREEN.
- RED T-001 confirmé: `tests/m004/validate_m004_precondition_acceptance.ps1` échouait sur l'absence de `scripts/validate_m004_precondition.ps1`; `tests/m003/validate_m003_precondition_unit.ps1` échouait tant que la branche M-004 post-merge n'était pas explicitement autorisée.
- Implémentation: `scripts/validate_m004_precondition.ps1` vérifie les branches autorisées `master` et `codex/milestone-m004-version-canonique-publiee`, la présence de M-000 à M-003 dans `master`, la relation `master` contient `origin/master`, la présence de `master` dans la branche courante, les gates `test` et `lint`, et écrit `docs/governance/m004_precondition_green.md`.
- Correction M-003: `scripts/validate_m003_precondition.ps1` autorise explicitement le post-merge sur `master` et la branche M-004 sans dépendre silencieusement de l'ancienne branche `codex/milestone-m003-source-routee`.
- ADR: non requise; T-001 applique ADR-010 et rend la précondition post-merge explicite sans changer la politique durable des gates PowerShell.
- Risques traités: l'ancien RED M-003 n'est pas supprimé ni masqué; un milestone amont absent, une branche non autorisée, une référence `master` qui ne contient pas `origin/master`, une gate RED ou un rapport hors dépôt restent refusés explicitement.

## Clôture T-002

- Scénario BDD: Given une source M-003 enregistrée, diagnostiquée et routée page par page; When la commande documentaire `POST /v1/documents/{id}/convert` est spécifiée pour produire la version canonique; Then le domaine publie une `CanonicalSource` immuable, construite par fusion pagewise vers un `DoclingDocument` unique, contrôlée avant et après conversion, et exclut explicitement les projections M-005.
- RED T-002 confirmé: `tests/m004/validate_m004_specification_acceptance.ps1` échouait sur l'absence de `scripts/validate_m004_specification.ps1`; `tests/m004/validate_m004_specification_unit.ps1` échouait ensuite sur la même absence avant l'implémentation.
- Implémentation: `docs/specs/m004_version_canonique_publiee.md` publie la spécification M-004 avec l'agrégat `CanonicalSource`, la fusion pagewise vers un `DoclingDocument` unique, les politiques `TextAuthoritySelectionPolicy`, `CanonicalAcceptancePolicy` et `CriticalPageSamplingPolicy`, les états, événements, QA pré et post-conversion, le contrat HTTP `POST /v1/documents/{id}/convert` et les exclusions M-005.
- Validateur: `scripts/validate_m004_specification.ps1` refuse l'absence des sections et marqueurs normatifs, les politiques incomplètes, les états ou événements manquants, les comportements sans scénario/test/ADR/commande, les fallbacks silencieux, l'omission de page, la mutation en place, la projection Qdrant prématurée et les formats non canoniques.
- Gates: `scripts/test.ps1` et `scripts/lint.ps1` enrôlent le validateur M-004 et les tests M-004; `docs/traceability/matrix.md` relie `REQ-M004-002` à la tâche, aux tests, au validateur et aux ADR appliquées.
- ADR: non requise; T-002 applique ADR-001, ADR-002, ADR-003, ADR-004 et DDD-ADR-003 sans introduire de décision structurante nouvelle.
- Validations GREEN: `tests/m004/validate_m004_specification_acceptance.ps1`; `tests/m004/validate_m004_specification_unit.ps1`; `scripts/validate_m004_specification.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1`.
- Risques résiduels: la tâche publie la spécification et son validateur; l'implémentation métier de conversion, adjudication, publication, `SourceLocator` et événement `CanonicalSourcePublished` reste portée par T-003 à T-010.

## Clôture T-003

- Scénario BDD: Given un `DocumentProcessingRun` M-003 avec un `RoutePlan` approuvé pour toutes les pages; When la conversion documentaire M-004 est demandée; Then chaque page est convertie uniquement par la route explicitement planifiée, chaque sortie conserve route, outil, version, hash et justification, puis la fusion pagewise crée un `DoclingDocument` unique avec ordre, item ids, labels, coordonnées et provenance.
- Commit RED: `8ba1755` (`test(m004): couvrir la conversion et fusion pagewise`).
- Commit GREEN: `d594660e96d416ecc9c90dd1b83ef43810a4161d` (`feat(m004): convertir et fusionner les pages routees`).
- Implémentation: `app/source_processing/domain/page_conversion.py` représente strictement les sorties pagewise, artefacts OCRmyPDF conditionnels, items canoniques et provenance compatible SourceLocator; `app/source_processing/application/convert_routed_pages.py` orchestre les ports Docling standard, Granite-Docling et OCRmyPDF uniquement depuis les routes M-003.
- Garde-fous livrés: tentative non `ROUTE_PLANNED` refusée; source en quarantaine refusée; route, outil, version et hash obligatoires; aucun fallback Docling vers Granite après erreur; ordre strict des pages; item ids canoniques uniques; coordonnées normalisées; provenance obligatoire; original immuable référencé sans modification.
- Gates: `scripts/test.ps1` enrôle `tests/m004/validate_page_conversion_acceptance.ps1` et `tests/m004/validate_page_conversion_unit.ps1`; `docs/traceability/matrix.md` relie `REQ-M004-003` à la tâche, au test d'acceptation, au code applicatif et aux ADR appliquées.
- ADR: non requise; T-003 applique ADR-001, ADR-002, ADR-003, ADR-004 et DDD-ADR-003 sans créer de nouvelle route normative, dépendance structurante ou politique durable.
- Validations GREEN: `tests/m004/validate_page_conversion_acceptance.ps1`; `tests/m004/validate_page_conversion_unit.ps1`; `scripts/validate_architecture_boundaries.ps1`; `scripts/validate_traceability.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `scripts/test.ps1`; `scripts/lint.ps1`.
- Risques résiduels: T-003 produit la conversion et la fusion pagewise; l'adjudication d'autorité textuelle, la QA, la publication immuable, la résolution SourceLocator et l'événement `CanonicalSourcePublished` restent portés par T-004 à T-010.

## Clôture T-004

- Scénario BDD: Given une page avec une sortie native et une sortie Granite qui divergent; When `TextAuthoritySelectionPolicy` arbitre avec une sélection explicite et une justification; Then une seule autorité textuelle est retenue, les candidats concurrents restent audités et une décision absente ou ambiguë bloque la fusion canonique.
- Commit RED: `c5208a6` (`test(m004): couvrir l autorite textuelle par page`).
- Commit GREEN: `4bcf57045c86517a199b43d3cf3c113837ba8c04` (`feat(m004): adjuger l autorite textuelle par page`).
- Implémentation: `app/source_processing/domain/page_conversion.py` ajoute `PageConversionCandidate`, `TextAuthority`, `TextAuthoritySelectionPolicy`, `TextAuthorityManifest` et la fusion `merge_authorized`, qui alimente le `DoclingDocument` uniquement avec les sorties explicitement retenues par le manifeste d'autorité.
- Garde-fous livrés: version de politique obligatoire; justification obligatoire; candidat source obligatoire; candidats concurrents conservés; une seule autorité par page du manifeste; refus `PAGE_AUTHORITY_MISSING` pour autorité absente ou page publiée sans autorité; refus `PAGE_AUTHORITY_AMBIGUOUS` pour sélection multiple, candidat dupliqué ou décision de page dupliquée.
- Gates: `scripts/test.ps1` enrôle `tests/m004/validate_text_authority_acceptance.ps1` et `tests/m004/validate_text_authority_unit.ps1`; `docs/traceability/matrix.md` relie `REQ-M004-004` à la tâche, au test d'acceptation, au code de domaine et à ADR-004. Les assertions de volume de gate ont été réalignées sur 79 tests globaux et 77 tests dans le rapport M-003 imbriqué.
- ADR: non requise; T-004 applique ADR-004 sans autoriser plusieurs autorités par page et sans changer la politique durable d'autorité textuelle unique.
- Validations GREEN: `tests/m004/validate_text_authority_acceptance.ps1`; `tests/m004/validate_text_authority_unit.ps1`; `tests/m004/validate_page_conversion_acceptance.ps1`; `tests/m004/validate_page_conversion_unit.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `scripts/validate_m004_specification.ps1`; `scripts/validate_traceability.ps1`; `scripts/validate_architecture_boundaries.ps1`; `scripts/test.ps1`; `scripts/lint.ps1`.
- Risques résiduels: T-004 ne publie pas encore `CanonicalSource`, ne réalise pas la QA documentaire finale, ne rend pas `SourceLocator` résolvable et n'émet pas `CanonicalSourcePublished`; ces comportements restent portés par T-005 à T-010.

## Clôture T-005

- Scénario BDD: Given une source routée contient une page à faible confiance et une table financière dont une conversion perd un signe négatif; When la QA pré-conversion puis post-conversion M-004 est exécutée; Then la page critique est sélectionnée explicitement, la relance de route éventuelle est tracée comme `RETRY_WITH_ALTERNATIVE_ROUTE`, la version canonique candidate est refusée avec anomalie explicite et aucun événement de publication n'est produit.
- Commit RED: `500af18` (`test(m004): couvrir le controle qualite canonique`).
- Commit GREEN: `9d14598be58b1ea2ae225e7ed6a3a3e68e1c73a6` (`feat(m004): controler la qualite de version canonique`).
- Implémentation: `app/source_processing/domain/page_conversion.py` ajoute `CriticalPageSamplingPolicy`, `PreConversionQualityReport`, `PostConversionQualityReport`, `PostConversionQualityFinding`, `QualityDecisionStatus`, `QualityFindingCode`, `CanonicalAcceptancePolicy` et `CanonicalQualityDecision`.
- Garde-fous livrés: seuil de faible confiance explicite; pages critiques versionnées et motivées; comparaison de route conservée comme décision métier; rapports QA pré et post-conversion obligatoires; source en quarantaine refusée; page omise, incohérence numérique, signe altéré et tableau incomplet bloquent la publication; anomalies conservées; payload d'audit sans texte documentaire complet.
- Gates: `scripts/test.ps1` enrôle `tests/m004/validate_canonical_quality_acceptance.ps1` et `tests/m004/validate_canonical_quality_unit.ps1`; `docs/traceability/matrix.md` relie `REQ-M004-005` à la tâche, au test d'acceptation, au code de domaine et aux ADR appliquées. Les assertions de volume de gate ont été réalignées sur 81 tests globaux, 80 tests dans le rapport M-004 imbriqué et 79 tests dans le rapport M-003 imbriqué.
- ADR: non requise; T-005 applique ADR-001, ADR-002, ADR-003 et ADR-004 sans modifier l'autorité canonique ni introduire une nouvelle stratégie structurante.
- Validations GREEN: `tests/m004/validate_canonical_quality_acceptance.ps1`; `tests/m004/validate_canonical_quality_unit.ps1`; `tests/m004/validate_text_authority_acceptance.ps1`; `tests/m004/validate_text_authority_unit.ps1`; `tests/m004/validate_page_conversion_acceptance.ps1`; `tests/m004/validate_page_conversion_unit.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `scripts/validate_m004_specification.ps1`; `scripts/validate_traceability.ps1`; `scripts/validate_architecture_boundaries.ps1`; `scripts/test.ps1`; `scripts/lint.ps1`.
- Risques résiduels: T-005 décide l'acceptation qualité et bloque les candidates RED; la publication immuable, la résolution `SourceLocator`, l'événement `CanonicalSourcePublished`, le contrat HTTP final et les gates M-004 de clôture restent portés par T-006 à T-010.

## Clôture T-006

- Scénario BDD: Given une source routée, convertie, adjugée et validée par QA; When la publication canonique est demandée; Then une version canonique immutable est créée avec `CanonicalSourceRef`, hash d'artefact, page count et statut publié, sans modifier les versions antérieures.
- Commit RED: `683b6dc` (`test(m004): couvrir la publication canonique immuable`).
- Commit GREEN: `3cae8478efee0b80044d9c5e72912cb011830496` (`feat(m004): publier une version canonique immuable`).
- Implémentation: `app/source_processing/domain/canonical_source.py` ajoute l'agrégat `CanonicalSource`, les versions canoniques immuables, les artefacts Docling JSON canoniques, les exports régénérables non canoniques et la production d'un `CanonicalSourceRef` public; `app/source_processing/application/publish_canonical_source.py` sérialise le `DoclingDocument` contrôlé, calcule son hash et appelle un port de stockage d'artefact.
- Garde-fous livrés: QA GREEN obligatoire; source en quarantaine refusée; correction par nouvelle version; mutation en place interdite; hash d'artefact canonique obligatoire; Markdown et HTML interdits comme source de vérité; exports régénérables sans modification de la référence canonique.
- Gates: `scripts/test.ps1` enrôle `tests/m004/validate_canonical_publication_acceptance.ps1` et `tests/m004/validate_canonical_publication_unit.ps1`; `docs/traceability/matrix.md` relie `REQ-M004-006` à la tâche, au test d'acceptation, au code de domaine/applicatif et aux ADR appliquées. Les assertions de volume de gate ont été réalignées sur 83 tests globaux, 82 tests dans le rapport M-004 imbriqué et 81 tests dans le rapport M-003 imbriqué.
- ADR: non requise; T-006 applique ADR-001 et DDD-ADR-003 sans modifier la source de vérité documentaire ni le langage publié `CanonicalSourceRef`.
- Validations GREEN: `tests/m004/validate_canonical_publication_acceptance.ps1`; `tests/m004/validate_canonical_publication_unit.ps1`; `tests/m001/validate_source_contracts_acceptance.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1`.
- Risques résiduels: T-006 publie l'agrégat et la référence canonique; la résolution fine `SourceLocator`, l'événement `CanonicalSourcePublished`, le contrat HTTP final et les gates M-004 de clôture restent portés par T-007 à T-010.

## Clôture T-007

- Scénario BDD: Given une version canonique publiée contenant une page avec plusieurs items; When un contexte aval valide un `SourceLocator` vers un item précis; Then SP confirme la version, la page PDF, l'item et le `content_hash`, ou refuse explicitement le locator.
- Commit RED: `adaaf93` (`test(m004): couvrir la resolution des source locator`).
- Commit GREEN: `42d3221c0c86ab87128b29a3d0a3a40913d5a97c` (`feat(m004): rendre les source locator resolvables`).
- Implémentation: `app/source_processing/application/source_locator_resolution.py` génère un registre de résolvabilité par version canonique, extrait les couples page PDF, `item_id`, `bbox` et `content_hash` du `PagewiseDoclingDocument`, produit une `SourceLocatorValidationPolicy` et résout le locator exact sans exposer les chemins d'artefacts, le stockage SP ni le texte documentaire complet.
- Garde-fous livrés: `item_id` stable; `content_hash` obligatoire; page hors version refusée par le contrat M-001; item absent, hash incohérent, page incohérente avec l'item, bbox incohérente, version absente, retirée ou en quarantaine refusés explicitement; aucun accès direct de KA, EG ou RA aux tables internes SP.
- Gates: `scripts/test.ps1` enrôle `tests/m004/validate_source_locator_resolution_acceptance.ps1` et `tests/m004/validate_source_locator_resolution_unit.ps1`; `docs/traceability/matrix.md` relie `REQ-M004-007` à la tâche, au test d'acceptation, au code applicatif et à DDD-ADR-003. Les assertions de volume de gate ont été réalignées sur 85 tests globaux, 84 tests dans le rapport M-004 imbriqué et 83 tests dans le rapport M-003 imbriqué.
- ADR: non requise; T-007 applique DDD-ADR-003 et le contrat M-001 `SourceLocator` sans modifier leur sens ni le langage publié de citation.
- Validations GREEN: `tests/m004/validate_source_locator_resolution_acceptance.ps1`; `tests/m004/validate_source_locator_resolution_unit.ps1`; `tests/m001/validate_source_locator_unit.ps1`; `scripts/validate_architecture_boundaries.ps1` avec paramètres; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1`.
- Risques résiduels: T-007 rend les citations ouvrables au niveau version, page et item; l'événement `CanonicalSourcePublished`, le contrat HTTP final et les gates M-004 de clôture restent portés par T-008 à T-010.

## Clôture T-008

- Scénario BDD: Given une version canonique vient d'être publiée par SP; When l'intégration intercontextes est traitée; Then un événement `CanonicalSourcePublished` versionné est inscrit dans l'outbox avec un payload `CanonicalSourceRef` et aucune donnée interne SP.
- Commit RED: `6e80443` (`test(m004): couvrir l evenement canonical source published`).
- Commit GREEN: `62d1248d2fbc201cc99dcc015580cd489f549cfb` (`feat(m004): publier l evenement canonical source published`).
- Implémentation: `app/source_processing/application/publish_canonical_source_event.py` construit une `EventEnvelope` M-001 uniquement depuis le `CanonicalSourceRef` public produit par T-006, calcule un `event_id` déterministe par `canonical_version_id`, associe une `ProducerStateMutation` cohérente et inscrit l'événement dans l'outbox M-002.
- Garde-fous livrés: événement impossible avant résultat de publication accepté; QA RED bloquée par la publication T-006 avant tout événement; retry idempotent retournant l'entrée outbox existante; refus d'un payload interne SP; absence de chemins d'artefacts, détails de conversion, texte documentaire ou appel direct KA/EG dans le payload.
- Gates: `scripts/test.ps1` enrôle `tests/m004/validate_canonical_publication_event_acceptance.ps1` et `tests/m004/validate_canonical_publication_event_unit.ps1`; `docs/traceability/matrix.md` relie `REQ-M004-008` à la tâche, au test d'acceptation, au code applicatif et aux ADR appliquées. Les assertions de volume de gate ont été réalignées sur 87 tests globaux, 86 tests dans le rapport M-004 imbriqué et 85 tests dans le rapport M-003 imbriqué.
- ADR: non requise; T-008 applique le contrat M-001 `EventEnvelope`, l'outbox M-002 et le `CanonicalSourceRef` M-001 sans changer la coordination intercontextes.
- Validations GREEN: `tests/m004/validate_canonical_publication_event_acceptance.ps1`; `tests/m004/validate_canonical_publication_event_unit.ps1`; `tests/m001/validate_event_envelope_acceptance.ps1`; `tests/m002/validate_outbox_acceptance.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1`.
- Risques résiduels: T-008 publie l'événement outbox; le contrat HTTP final de conversion documentaire et les gates M-004 de clôture restent portés par T-009 et T-010.

## Clôture T-009

- Scénario BDD: Given un document enregistré, diagnostiqué et routé; When un client appelle `POST /v1/documents/{id}/convert`; Then la conversion canonique est acceptée comme job `CONVERT_DOCUMENT` idempotent ou refusée par erreur publique stable sans exposer de structure interne SP.
- Commit RED: `85191e6` (`test(m004): couvrir la commande de conversion documentaire`).
- Commit GREEN: `20f65cf6b9ea98832be8ab16c30c1a633d8b666a` (`feat(m004): exposer la commande de conversion documentaire`).
- Implémentation: `app/source_processing/application/document_commands.py` ajoute `DocumentConversionState`, `DocumentConversionAcceptance`, les erreurs métier de conversion et la commande `request_document_conversion`; `app/source_processing/adapters/document_http.py` expose `POST /v1/documents/{id}/convert`; `app/platform/job_runtime/__init__.py` déclare explicitement `CONVERT_DOCUMENT`.
- Garde-fous livrés: source inconnue refusée; source en quarantaine refusée; tentative non `ROUTE_PLANNED` refusée; conversion déjà demandée refusée; QA canonique refusée exposée comme `SOURCE_NOT_CANONICAL`; corps HTTP de conversion non vide refusé; `/diagnose` ne déclenche aucun job de conversion; aucune route, autorité, `job_id`, `processing_run_id`, hash ou stockage interne n'est exposé dans la réponse publique.
- Gates: `scripts/test.ps1` enrôle `tests/m004/validate_document_conversion_command_acceptance.ps1` et `tests/m004/validate_document_conversion_command_unit.ps1`; les assertions de volume de gate sont réalignées sur 89 tests globaux et 87 tests dans le rapport M-003 imbriqué; `docs/traceability/matrix.md` relie `REQ-M004-009` à la tâche, au test d'acceptation, au code applicatif/HTTP et aux ADR appliquées.
- ADR: non requise; T-009 applique l'endpoint prévu par M-004, le langage publié documentaire, l'idempotence M-002 et les gates ADR-010 sans créer de nouvelle frontière structurante.
- Validations GREEN: `tests/m004/validate_document_conversion_command_acceptance.ps1`; `tests/m004/validate_document_conversion_command_unit.ps1`; `tests/m003/validate_document_http_contract_acceptance.ps1`; `scripts/validate_architecture_boundaries.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1`.
- Risques résiduels: T-009 expose la commande de conversion; la clôture complète M-004, les preuves finales et les gates de fin restent portées par T-010.

## Clôture T-010

- Scénario BDD: Given les comportements M-004 sont implémentés et testés; When les gates de clôture sont exécutées; Then chaque exigence M-004 est reliée à une preuve et la clôture est refusée si un test, une ADR, une commande, un locator ou un signal d'audit manque.
- Commit RED: `b32fe05` (`test(m004): couvrir la tracabilite de version canonique`).
- Commit GREEN: `45839de20b30d629ab9ac6ccd9345a1f940fac8b` (`docs(m004): relier m004 aux gates et a la tracabilite`).
- Implémentation: `scripts/validate_traceability.ps1` contrôle explicitement `REQ-M004-001` à `REQ-M004-010`, avec statut `Couvert`, test, commande PowerShell, code, ADR existantes et justification structurante; `docs/traceability/matrix.md` relie la précondition M-004 et la clôture T-010; `app/source_processing/application/canonical_audit_signals.py` produit les logs et métriques de clôture canonique.
- Métriques de clôture: `versions_canoniques_publiees`, `pages_refusees_qa`, `autorites_textuelles_ambigues` et `refus_canoniques` sont agrégées avec `scope=m004`, `phase`, `status` et `error_code` depuis des événements d'audit sans contenu documentaire complet.
- Garde-fous livrés: exigence M-004 absente refusée; statut non `Couvert` refusé; commande introuvable refusée; code manquant refusé; preuve de domaine ou adaptateur incohérente refusée; ADR incomplète refusée; clôture sans preuve d'audit canonique refusée; logs et métriques ne transportent pas de texte intégral de document.
- Gates: `scripts/test.ps1` enrôle `tests/m004/validate_m004_traceability_acceptance.ps1` et `tests/m004/validate_m004_traceability_unit.ps1`; les assertions de volume de gate sont réalignées sur 91 tests globaux, 90 tests dans le rapport M-004 imbriqué et 89 tests dans le rapport M-003 imbriqué.
- ADR: non requise; T-010 applique ADR-001, ADR-004, ADR-010, DDD-ADR-003, DDD-ADR-006 et DDD-ADR-008 sans changer la source de vérité canonique, l'autorité textuelle unique, la politique de gates, le langage `SourceLocator`, l'audit par état courant ou la cohérence éventuelle.
- Validations GREEN: `tests/m004/validate_m004_traceability_acceptance.ps1`; `tests/m004/validate_m004_traceability_unit.ps1`; `scripts/validate_traceability.ps1`; `python -m py_compile app/source_processing/application/canonical_audit_signals.py`; `scripts/test.ps1`; `scripts/lint.ps1`.
- Risques résiduels: aucun risque résiduel identifié pour T-010; M-004 reste à soumettre à la revue locale de milestone avant publication de PR.

## Correction de revue locale M-004

- Scénario BDD: Given une version canonique candidate ou corrigée; When la publication, la QA, les SourceLocator, l'événement outbox, l'audit et l'endpoint `/convert` sont exercés après revue; Then la publication est refusée sans effet de bord si l'autorité textuelle, la QA ou les contrats publics ne sont pas strictement satisfaits.
- Findings corrigés: publication impossible sans `TextAuthorityManifest`; stockage Docling JSON différé après validation de l'agrégat; `content_hash` recalculé depuis le texte; références d'artefacts pagewise durcies; QA post-conversion enrichie avec pages hors manifeste, divergence avec l'autorité textuelle, pourcentage altéré, séparateur décimal altéré et figure sans provenance; correction canonique accompagnée de `CanonicalSourceSuperseded`; outbox consommée via port minimal; `PAGE_AUTHORITY_MISSING` atteignable par `/convert`; réponses `/convert` sans raison interne; audit M-004 limité à des identifiants, phases et codes publics stricts.
- Documentation: la table d'événements M-004 précise que `CanonicalSourcePublished` publie le `CanonicalSourceRef` contractuel, avec `canonical_artifact_sha256` inclus, et que les `SourceLocator` sont résolus par le registre T-007.
- ADR: non requise; les corrections appliquent les contrats et décisions existants sans changer la source de vérité canonique, l'outbox M-002, le langage `CanonicalSourceRef` ni le modèle `SourceLocator`.
- Validations GREEN: `python -m py_compile app/source_processing/domain/page_conversion.py app/source_processing/domain/canonical_source.py app/source_processing/application/publish_canonical_source.py app/source_processing/application/publish_canonical_source_event.py app/source_processing/application/document_commands.py app/source_processing/adapters/document_http.py app/source_processing/application/canonical_audit_signals.py`; `git diff --check`; `tests/m003/validate_document_http_contract_acceptance.ps1`; `tests/m001/validate_source_locator_unit.ps1`; `tests/m001/validate_event_envelope_acceptance.ps1`; `tests/m002/validate_outbox_acceptance.ps1`; `scripts/validate_m004_specification.ps1`; `scripts/validate_traceability.ps1`; `scripts/test.ps1`; `scripts/lint.ps1`.

## Correction de revue locale M-004 - vague 2

- Scénario BDD: Given une correction canonique concurrente ou rejouée; When la publication applicative, l'inscription outbox, la résolution `SourceLocator`, l'audit et l'endpoint HTTP documentaire sont exercés; Then la branche publie uniquement des contrats cohérents, idempotents et résolubles sans état partiel ni rupture M-003.
- Findings corrigés: verrou optimiste obligatoire via `expected_current_version_id`; inscription atomique du lot `CanonicalSourcePublished` puis `CanonicalSourceSuperseded`; idempotence de course relue depuis l'outbox existante; payload `CanonicalSourceSuperseded` validé par `EventEnvelope`; anciennes versions corrigées conservées résolubles en statut `ACCEPTED`; adapter HTTP compatible avec un port M-003 sans conversion; contrat `/convert` documenté avec `CANONICAL_ACCEPTED` et `CONVERSION_ALREADY_REQUESTED`; audit `SOURCE_QUARANTINED` source-level accepté sans compteur artificiel; test de traçabilité aligné sur `scripts/require_python.ps1`.
- ADR: non requise; la correction renforce les contrats M-001, l'outbox M-002 et les invariants M-004 sans nouvelle décision structurante.
- Validations GREEN: `python -m py_compile app/platform/event_bus/outbox.py app/contracts/event_envelope.py app/source_processing/domain/canonical_source.py app/source_processing/application/publish_canonical_source.py app/source_processing/application/publish_canonical_source_event.py app/source_processing/adapters/document_http.py app/source_processing/application/canonical_audit_signals.py`; `tests/m001/validate_event_envelope_unit.ps1`; `tests/m001/validate_event_envelope_acceptance.ps1`; `tests/m002/validate_outbox_unit.ps1`; `tests/m002/validate_outbox_acceptance.ps1`; `tests/m003/validate_document_http_contract_acceptance.ps1`; `tests/m004/validate_canonical_publication_unit.ps1`; `tests/m004/validate_canonical_publication_acceptance.ps1`; `tests/m004/validate_canonical_publication_event_unit.ps1`; `tests/m004/validate_canonical_publication_event_acceptance.ps1`; `tests/m004/validate_source_locator_resolution_unit.ps1`; `tests/m004/validate_source_locator_resolution_acceptance.ps1`; `tests/m004/validate_document_conversion_command_unit.ps1`; `tests/m004/validate_document_conversion_command_acceptance.ps1`; `tests/m004/validate_m004_specification_unit.ps1`; `scripts/validate_m004_specification.ps1`; `tests/m004/validate_m004_traceability_acceptance.ps1`; `scripts/validate_traceability.ps1`; `git diff --check`; `scripts/lint.ps1`; `scripts/test.ps1` (`13 validation(s), 91 test(s)`).

## Correction de revue locale M-004 - vague 3

- Scénario BDD: Given les conversions, décisions QA, publications et refus pré-canoniques M-004; When la revue locale contrôle performance, observabilité, typage, conventions et documentation; Then les invariants sont vérifiés sans comparaison complète inutile, les refus sont auditables et la documentation trace les contrats réellement publiés.
- Findings corrigés: comparaison d'autorité textuelle par signatures de pages au lieu de payload complet; validation stricte des artefacts attendus pour conversion et prétraitement; invariants QA refusant un PASS incohérent; `original_storage_ref` propagé et vérifié dans les versions canoniques; politique `SourceLocator` mémorisée sans recalcul implicite; audit pré-canonique `PreCanonicalAuditEvent` pour `/convert` avec compteurs `refus_canoniques`; réponse `SOURCE_NOT_ROUTED` sans statut interne; spécification et matrice alignées sur les événements et ADR réellement utilisés.
- ADR: non requise; la correction applique ADR-001, ADR-004, ADR-010, DDD-ADR-003, DDD-ADR-006, DDD-ADR-008 et DDD-ADR-010 sans nouvelle décision structurante.
- Validations GREEN: `python -m py_compile app/source_processing/domain/page_conversion.py app/source_processing/application/convert_routed_pages.py app/source_processing/domain/canonical_source.py app/source_processing/application/source_locator_resolution.py app/source_processing/application/canonical_audit_signals.py app/source_processing/application/document_commands.py app/source_processing/adapters/document_http.py`; `tests/m004/validate_page_conversion_unit.ps1`; `tests/m004/validate_page_conversion_acceptance.ps1`; `tests/m004/validate_canonical_quality_unit.ps1`; `tests/m004/validate_canonical_publication_unit.ps1`; `tests/m004/validate_document_conversion_command_unit.ps1`; `tests/m004/validate_document_conversion_command_acceptance.ps1`; `tests/m004/validate_m004_traceability_acceptance.ps1`; `tests/m004/validate_m004_traceability_unit.ps1`; `tests/m004/validate_m004_specification_unit.ps1`; `scripts/validate_m004_specification.ps1`; `scripts/validate_traceability.ps1`; `git diff --check`; `scripts/lint.ps1` (`13 validation(s), 0 test(s)`); `scripts/test.ps1` (`13 validation(s), 91 test(s)`).
