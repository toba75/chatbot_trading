# Journal M-005 - Projection de connaissance recherchable

## Statut initial

- Planification créée depuis `docs/specs/plan_implementation_milestones_workstreams.md`, section `M-005 - Projection de connaissance recherchable`.
- Spécification normative consultée: `docs/specs/specification_unifiee_ddd_technique_chatbot_trading_v4_1.md`, sections KA, projections régénérables, chunking, indexation, recherche hybride, API et critères V1.
- Dépendance directe: M-004 - Version canonique publiée.
- Milestones amont vérifiés dans `master`: M-000, M-001, M-002, M-003 et M-004.
- Référence `master` inspectée après fast-forward: `4bd6ebb98ce109a828a5d02f378fb6c3fa50bfa9`.
- État initial des gates: `scripts/validate_task_system.ps1` GREEN; `scripts/lint.ps1` GREEN; `scripts/test.ps1` GREEN avec 13 validation(s) et 91 test(s).
- Dossier créé: `docs/tasks/milestone_005`.

## Ordre d'exécution prévu

1. T-001 - Vérifier et rétablir la précondition GREEN M-005.
2. T-002 - Publier la spécification de projection de connaissance.
3. T-003 - Créer une projection depuis une version canonique publiée et exposer `POST /v1/documents/{document_id}/index`.
4. T-004 - Découper le contenu canonique en chunks traçables.
5. T-005 - Enrichir les métadonnées de projection filtrable.
6. T-006 - Encoder la projection en dense et sparse.
7. T-007 - Publier un index Qdrant régénérable et les événements KA.
8. T-008 - Rechercher des preuves candidates hybrides avec trace persistée.
9. T-009 - Exposer la commande de recherche Knowledge Access.
10. T-010 - Relier M-005 aux métriques, à la traçabilité et aux gates.

## Suivi d'exécution

| Tâche | Commit RED | Commit GREEN | ADR consultées | ADR créée ou modifiée | Validations GREEN déclarées |
|---|---|---|---|---|---|
| T-001 - Vérifier et rétablir la précondition GREEN M-005 | `f397b8b04a21bbd908e6a99eaab37a99600565df` (`test(m005): couvrir la precondition green de projection`) | `0361a45f63990227ccf176cf25a76ca060f6d1cb` (`test(m005): etablir la precondition green de projection`) | ADR-010 | Aucune | `tests/m005/validate_m005_precondition_unit.ps1`; `tests/m005/validate_m005_precondition_acceptance.ps1`; `scripts/validate_m005_precondition.ps1`; `git diff --check`; `scripts/lint.ps1`; `scripts/test.ps1` (`13 validation(s), 93 test(s)`) |
| T-002 - Publier la spécification de projection de connaissance | `a7c7460261281861f869b687428405b132012f99` (`test(m005): couvrir la specification de projection`) | `66e6c39866497032d9344c792344d964fa97ca95` (`docs(m005): publier la specification de projection`) | ADR-005; ADR-006; ADR-010; DDD-ADR-004; DDD-ADR-008 | Aucune | `tests/m005/validate_m005_specification_acceptance.ps1`; `tests/m005/validate_m005_specification_unit.ps1`; `scripts/validate_m005_specification.ps1`; `scripts/validate_traceability.ps1`; `git diff --check`; `scripts/lint.ps1`; `scripts/test.ps1` (`14 validation(s), 95 test(s)`) |
| T-003 - Créer une projection depuis une version canonique publiée | `c3b7b1c474b3cb446074811c1ec4d76e9dae83ee` (`test(m005): couvrir la creation de projection`) | `f7cb9b8ee079d1951ad5c55dc4be173f4d7a52b5` (`feat(m005): creer la projection depuis source canonique`) | ADR-010; DDD-ADR-004; DDD-ADR-008 | Aucune | `tests/m005/validate_knowledge_projection_acceptance.ps1`; `tests/m005/validate_knowledge_projection_unit.ps1`; `tests/m005/validate_index_command_acceptance.ps1`; `scripts/validate_architecture_boundaries.ps1`; `scripts/validate_traceability.ps1`; `git diff --check`; `scripts/lint.ps1`; `scripts/test.ps1` (`14 validation(s), 98 test(s)`) |
| T-004 - Découper le contenu canonique en chunks traçables | `6e0480b575ede03d773409d4e4f7799ab0602736` (`test(m005): couvrir le chunking tracable`) | `32cffee027158a92f0e6f6fbda26b8da926549ad` (`feat(m005): decouper le contenu canonique en chunks`) | ADR-001; ADR-006; DDD-ADR-003; DDD-ADR-004 | Aucune | `tests/m005/validate_hierarchical_chunking_acceptance.ps1`; `tests/m005/validate_hierarchical_chunking_unit.ps1`; `tests/m004/validate_source_locator_resolution_acceptance.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `git diff --check`; `scripts/lint.ps1`; `scripts/test.ps1` (`14 validation(s), 100 test(s)`) |
| T-005 - Enrichir les métadonnées de projection filtrable | `56b841c247575ea9bf5a111acc94a46313cb0f88` (`test(m005): couvrir les filtres de projection`) | `a98b8b2e2057e723948d27c1d8ee6edb80e5dd14` (`feat(m005): enrichir les metadonnees filtrables`) | ADR-005; DDD-ADR-004 | Aucune | `tests/m005/validate_projection_metadata_filters_acceptance.ps1`; `tests/m005/validate_projection_metadata_filters_unit.ps1`; `scripts/validate_m005_specification.ps1`; `scripts/validate_traceability.ps1`; `git diff --check`; `scripts/lint.ps1`; `scripts/test.ps1` (`14 validation(s), 102 test(s)`) |
| T-006 - Encoder la projection en dense et sparse | `c19cf7fb76b5d43b1d0b7c3eb0ad0be32d07dd68` (`test(m005): couvrir l encodage dense sparse`) | `9d6cc48ddfd457f7a572c00e4cb33b8fb87626ea` (`feat(m005): encoder la projection dense sparse`) | ADR-005; ADR-007; ADR-009; DDD-ADR-004 | Aucune | `tests/m005/validate_projection_encoding_acceptance.ps1`; `tests/m005/validate_projection_encoding_unit.ps1`; `scripts/validate_network_boundary.ps1`; `scripts/validate_traceability.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `tests/m004/validate_m004_traceability_acceptance.ps1`; `tests/governance/validate_m000_validation_commands_acceptance.ps1`; `git diff --check`; `scripts/lint.ps1`; `scripts/test.ps1` (`14 validation(s), 104 test(s)`) |
| T-007 - Publier un index Qdrant régénérable | `00215fe8e1c8487d0a0e3b0f758488e84093f3ff` (`test(m005): couvrir l index qdrant regenerable`) | `6154715f05001a9c10143cca86882874da797ff9` (`feat(m005): publier l index qdrant regenerable`) | ADR-005; DDD-ADR-004; DDD-ADR-008 | Aucune | `tests/m005/validate_qdrant_projection_acceptance.ps1`; `tests/m005/validate_qdrant_projection_unit.ps1`; `tests/m005/validate_knowledge_projection_events_acceptance.ps1`; `scripts/validate_architecture_boundaries.ps1`; `scripts/validate_traceability.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `tests/m004/validate_m004_traceability_acceptance.ps1`; `git diff --check`; `scripts/lint.ps1`; `scripts/test.ps1` (`14 validation(s), 107 test(s)`) |
| T-008 - Rechercher des preuves candidates hybrides | `f20ed12f9e633da3dd42e437ff013e1986877048` (`test(m005): couvrir la recherche hybride tracable`) | `92a59b5fe69b11b74273586d55035014a92d8585` (`feat(m005): rechercher des preuves candidates hybrides`) | ADR-005; ADR-006; DDD-ADR-003; DDD-ADR-004; DDD-ADR-008 | Aucune | `tests/m005/validate_hybrid_search_acceptance.ps1`; `tests/m005/validate_hybrid_search_unit.ps1`; `tests/m005/validate_search_trace_acceptance.ps1`; `tests/m004/validate_source_locator_resolution_acceptance.ps1`; `scripts/validate_traceability.ps1`; `git diff --check`; `scripts/lint.ps1`; `scripts/test.ps1` (`14 validation(s), 110 test(s)`) |
| T-009 - Exposer la commande de recherche Knowledge Access | `edbe6c8` (`test(m005): couvrir la commande de recherche`) | `1a240d2` (`feat(m005): exposer la recherche knowledge access`) | ADR-005; ADR-006; ADR-010; DDD-ADR-003; DDD-ADR-004 | Aucune | `tests/m005/validate_search_command_acceptance.ps1`; `tests/m005/validate_search_command_unit.ps1`; `tests/m005/validate_hybrid_search_acceptance.ps1`; `scripts/validate_traceability.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `tests/m004/validate_m004_traceability_acceptance.ps1`; `git diff --check`; `scripts/lint.ps1`; `scripts/test.ps1` (`14 validation(s), 112 test(s)`) |
| T-010 - Relier M-005 aux métriques, à la traçabilité et aux gates | `ff74e3dd8ab4ee8fb08c0985aa6c20ff0fecd034` (`test(m005): couvrir la tracabilite de projection`) | `3d93e613df549c546061f63e20e2f3cd81e2ba01` (`docs(m005): relier m005 aux gates et metriques`) | ADR-005; ADR-006; ADR-010; DDD-ADR-004; DDD-ADR-008 | Aucune | `tests/m005/validate_m005_traceability_acceptance.ps1`; `tests/m005/validate_m005_traceability_unit.ps1`; `scripts/validate_traceability.ps1`; `git diff --check`; `scripts/lint.ps1`; `scripts/test.ps1` (`14 validation(s), 114 test(s)`) |

## Garde-fous de planification

- Qdrant reste une projection KA régénérable, jamais une source de vérité documentaire.
- KA retourne des preuves candidates, jamais des claims vérifiés ni des réponses rédigées.
- RA et EG doivent consommer `KnowledgeSearchPort`; aucun accès direct à Qdrant n'est planifié.
- `POST /v1/documents/{document_id}/index` appartient à KA et ne doit pas être confondu avec les endpoints SP.
- Les événements `KnowledgeProjectionBuilt`, `KnowledgeProjectionBecameSearchable`, `KnowledgeProjectionFailed`, `KnowledgeProjectionBecameStale` et `KnowledgeProjectionRetired` doivent être testés dès que les transitions correspondantes sont livrées.
- Toute recherche auditable doit persister une trace contenant paramètres, versions de projection, modèles, profils, filtres et avertissements de fraîcheur.
- Une source en quarantaine ou non canonique est non indexable.
- Une projection `STALE` ne doit pas être utilisée silencieusement lorsqu'une projection actuelle est requise.
- Les métriques Recall@k, MRR et nDCG initiales servent de mesure M-005 et non de seuil d'acceptation V1 avant calibration M-012.

## Clôture T-001

- Scénario BDD: Given M-000, M-001, M-002, M-003 et M-004 sont présents dans `master`; When les gates de précondition M-005 sont exécutées; Then M-005 ne commence que si `test`, `lint`, la traçabilité, les ADR, les frontières d'architecture et les preuves M-004 sont GREEN.
- RED T-001 confirmé: `tests/m005/validate_m005_precondition_acceptance.ps1` échouait tant que `scripts/validate_m005_precondition.ps1`, le rapport `docs/governance/m005_precondition_green.md` et l'autorisation de la branche M-005 dans les préconditions M-003/M-004 n'étaient pas livrés.
- Implémentation: `scripts/validate_m005_precondition.ps1` vérifie les branches autorisées `master` et `codex/milestone-m005-projection-connaissance`, la présence de M-000 à M-004 dans `master`, la relation `master`/`origin/master`, la présence de `master` dans la branche courante, les gates `test` et `lint`, puis écrit `docs/governance/m005_precondition_green.md`.
- Correction amont: les préconditions M-003 et M-004 autorisent explicitement la branche post-merge M-005 sans masquer leurs refus existants; `scripts/test.ps1` évite la récursion des préconditions imbriquées via des variables d'environnement dédiées.
- ADR: non requise; T-001 applique ADR-010 et rend le gate de départ M-005 explicite sans changer la politique durable des gates PowerShell.
- Validations GREEN: `tests/m005/validate_m005_precondition_unit.ps1`; `tests/m005/validate_m005_precondition_acceptance.ps1`; `git diff --check`; `scripts/lint.ps1` (`13 validation(s), 0 test(s)`); `scripts/test.ps1` (`13 validation(s), 93 test(s)`).
- Risques résiduels: aucun risque résiduel identifié pour la précondition; l'implémentation fonctionnelle de projection, chunking, encodage, indexation et recherche reste portée par T-002 à T-010.

## Clôture T-002

- Scénario BDD: Given une version canonique M-004 publiée; When la spécification M-005 est publiée; Then chaque comportement de projection et recherche nomme son invariant, son scénario BDD, son test RED, ses ADR applicables et sa commande de validation.
- RED T-002 confirmé: `tests/m005/validate_m005_specification_acceptance.ps1` et `tests/m005/validate_m005_specification_unit.ps1` échouaient tant que `scripts/validate_m005_specification.ps1` et `docs/specs/m005_projection_connaissance_recherchable.md` étaient absents.
- Implémentation: `docs/specs/m005_projection_connaissance_recherchable.md` publie la mission KA, l'agrégat `KnowledgeProjection`, les états, objets-valeur, politiques, ports, événements KA, API publiques, erreurs, métriques, exclusions M-006/M-007 et comportements exécutables KA-001 à KA-009.
- Garde-fous publiés: Qdrant reste une projection régénérable; aucun claim EG n'est indexé dans l'index documentaire; RA consomme `KnowledgeSearchPort` sans accès direct à Qdrant; aucun score n'est traité comme vérité métier.
- Gates: `scripts/validate_m005_specification.ps1` est enrôlé dans `scripts/lint.ps1` et `scripts/test.ps1`; la matrice de traçabilité couvre `REQ-M005-002`; les compteurs de tests impactés ont été alignés sur le nouveau volume du gate.
- ADR: non requise; T-002 applique ADR-005, ADR-006, ADR-010, DDD-ADR-004 et DDD-ADR-008 sans changer leur sens.
- Validations GREEN: `tests/m005/validate_m005_specification_acceptance.ps1`; `tests/m005/validate_m005_specification_unit.ps1`; `scripts/validate_m005_specification.ps1`; `scripts/validate_traceability.ps1`; `git diff --check`; `scripts/lint.ps1` (`14 validation(s), 0 test(s)`); `scripts/test.ps1` (`14 validation(s), 95 test(s)`).
- Risques résiduels: aucun risque résiduel identifié pour la publication de spécification; l'implémentation applicative de projection, chunking, encodage, indexation et recherche reste portée par T-003 à T-009, puis reliée aux gates par T-010.

## Clôture T-003

- Scénario BDD: Given une `CanonicalSource` publiée et non mise en quarantaine; When KA reçoit `POST /v1/documents/{document_id}/index` avec un profil d'indexation explicite; Then une `KnowledgeProjection` `REQUESTED` est créée avec une empreinte de build unique et aucune donnée canonique n'est modifiée.
- RED T-003 confirmé: `tests/m005/validate_knowledge_projection_acceptance.ps1`, `tests/m005/validate_knowledge_projection_unit.ps1` et `tests/m005/validate_index_command_acceptance.ps1` échouaient sur le module KA de projection absent avant l'implémentation.
- Implémentation: `app/knowledge_access/domain/knowledge_projection.py` livre `KnowledgeProjection`, `ProjectionProfile`, `BuildFingerprint` et les transitions initiales; `app/knowledge_access/application/request_projection.py` livre `RequestKnowledgeProjection`, `ProjectionEligibilityPolicy`, les ports de lecture canonique et la consommation idempotente de `CanonicalSourcePublished`; `app/knowledge_access/adapters/projection_http.py` expose `POST /v1/documents/{document_id}/index`; `app/knowledge_access/adapters/in_memory_projection_repository.py` livre le repository KA minimal.
- Garde-fous livrés: source en quarantaine ou non canonique refusée sans `202`; profils `chunking`, `embedding`, `sparse` et `index` obligatoires; corps ambigu refusé; projection identique refusée sans duplication; aucune mutation SP; aucune transition directe vers `SEARCHABLE`.
- Traçabilité et gates: `REQ-M005-003` a été ajoutée à `docs/traceability/matrix.md`; `scripts/test.ps1` enrôle les trois tests T-003; les compteurs de tests de gouvernance ont été alignés sur `98 test(s)`.
- ADR: non requise; T-003 applique ADR-010, DDD-ADR-004 et DDD-ADR-008 sans changer leur sens.
- Validations GREEN: `tests/m005/validate_knowledge_projection_acceptance.ps1`; `tests/m005/validate_knowledge_projection_unit.ps1`; `tests/m005/validate_index_command_acceptance.ps1`; `scripts/validate_architecture_boundaries.ps1`; `scripts/validate_traceability.ps1` (`56 exigence(s)`); `git diff --check`; `scripts/lint.ps1` (`14 validation(s), 0 test(s)`); `scripts/test.ps1` (`14 validation(s), 98 test(s)`).
- Risques résiduels: T-003 ne persiste pas encore hors mémoire et ne construit pas les chunks, encodages ou index Qdrant; ces travaux restent portés par T-004 à T-007. La projection livrée reste donc volontairement au statut `REQUESTED`.

## Clôture T-004

- Scénario BDD: Given une version canonique publiée avec pages, items et hashes; When KA applique un profil de chunking hiérarchique explicite; Then chaque chunk porte ses pages, ses item ids, son `SourceLocator` résolvable et un `content_hash` cohérent.
- RED T-004 confirmé: `tests/m005/validate_hierarchical_chunking_acceptance.ps1` et `tests/m005/validate_hierarchical_chunking_unit.ps1` échouaient sur l'absence de `app.knowledge_access.domain.chunking`; la boucle applicative a ensuite confirmé l'absence de `app.knowledge_access.application.chunk_canonical_source` avant livraison du handler.
- Implémentation: `app/knowledge_access/domain/chunking.py` livre `ChunkingProfile`, `CanonicalChunkDocument`, `CanonicalChunkItem`, `KnowledgeChunk`, `HierarchicalChunkProjection` et `HierarchicalChunkProjector`; `app/knowledge_access/application/chunk_canonical_source.py` livre `ProjectCanonicalChunksHandler` via `CanonicalSourceReader.find_chunking_source_by_version_id`.
- Garde-fous livrés: limites de taille explicites; profil versionné; parent obligatoire pour chaque enfant; refus d'item sans `SourceLocator`; refus de document canonique non publié; refus de texte brut `raw_text`; vérification du hash item/texte; aucun chunk sans source; aucun champ de claim stocké ou sérialisé.
- Traçabilité et gates: `scripts/test.ps1` enrôle les deux tests T-004; `tests/m003/validate_m003_precondition_acceptance.ps1` aligne le compteur imbriqué sur `96 test(s)` après ajout des deux tests T-004; la matrice de traçabilité n'a pas été modifiée.
- ADR: non requise; T-004 applique ADR-001, ADR-006, DDD-ADR-003 et DDD-ADR-004 sans changer leur sens.
- Validations GREEN: `tests/m005/validate_hierarchical_chunking_acceptance.ps1`; `tests/m005/validate_hierarchical_chunking_unit.ps1`; `tests/m004/validate_source_locator_resolution_acceptance.ps1`; `tests/m005/validate_knowledge_projection_acceptance.ps1`; `tests/m005/validate_knowledge_projection_unit.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `git diff --check`; `scripts/lint.ps1` (`14 validation(s), 0 test(s)`); `scripts/test.ps1` (`14 validation(s), 100 test(s)`).
- Risques résiduels: T-004 construit une projection de chunks en mémoire et ne publie pas encore les métadonnées filtrables, encodages dense/sparse ni l'index Qdrant; ces travaux restent portés par T-005 à T-007.

## Clôture T-005

- Scénario BDD: Given une projection contient des chunks issus de plusieurs documents et auteurs; When une recherche exige un filtre d'auteur, de période ou de type de contenu; Then seuls les chunks satisfaisant explicitement le filtre sont éligibles, avec une trace de filtre consultable.
- RED T-005 confirmé: `tests/m005/validate_projection_metadata_filters_acceptance.ps1` et `tests/m005/validate_projection_metadata_filters_unit.ps1` échouaient sur l'absence de `app.knowledge_access.domain.projection_metadata`.
- Implémentation: `app/knowledge_access/domain/projection_metadata.py` livre `ProjectionMetadata`, `SearchFilter`, `ProjectionFreshnessPolicy`, `EvidenceDiversificationPolicy`, la sélection `ProjectionMetadataSelector` et les traces de filtre, fraîcheur et diversification.
- Garde-fous livrés: filtre inconnu refusé par `FILTER_NOT_SUPPORTED`; projection `STALE` refusée quand une projection courante est requise; usage `STALE` seulement possible avec avertissement contractuel explicite; auteur, date, type de contenu et qualité canonique obligatoires; aucune dimension de filtre ignorée silencieusement; diversification par document tracée quand elle est demandée.
- Traçabilité et gates: `REQ-M005-004` a été ajouté à `docs/traceability/matrix.md`; `scripts/test.ps1` enrôle les deux tests T-005; `tests/m003/validate_m003_precondition_acceptance.ps1`, `tests/m004/validate_m004_traceability_acceptance.ps1` et `tests/governance/validate_m000_validation_commands_acceptance.ps1` alignent les compteurs et preuves de gate sur `57 exigence(s)` et `102 test(s)`.
- ADR: non requise; T-005 applique ADR-005 et DDD-ADR-004 sans changer leur sens.
- Validations GREEN: `tests/m005/validate_projection_metadata_filters_acceptance.ps1`; `tests/m005/validate_projection_metadata_filters_unit.ps1`; `scripts/validate_m005_specification.ps1`; `scripts/validate_traceability.ps1` (`57 exigence(s)`); `git diff --check`; `scripts/lint.ps1` (`14 validation(s), 0 test(s)`); `scripts/test.ps1` (`14 validation(s), 102 test(s)`); `tests/governance/validate_m000_validation_commands_acceptance.ps1`.
- Risques résiduels: T-005 ne livre pas l'encodage dense/sparse, l'index Qdrant ni la commande publique `POST /v1/search`; ces travaux restent portés par T-006 à T-009.

## Clôture T-006

- Scénario BDD: Given des chunks éligibles avec métadonnées et profil d'encodage explicite; When KA encode la projection; Then chaque chunk possède un résultat dense et sparse versionné ou la projection échoue avec un code explicite.
- RED T-006 confirmé: `tests/m005/validate_projection_encoding_acceptance.ps1` et `tests/m005/validate_projection_encoding_unit.ps1` échouaient sur l'absence de `app.knowledge_access.application.encode_projection` avant l'implémentation.
- Implémentation: `app/knowledge_access/domain/projection_encoding.py` livre les profils `DenseEncodingProfile`, `SparseEncodingProfile`, `ProjectionEncodingProfile`, les vecteurs, les résultats versionnés, la trace d'encodage et les erreurs `DENSE_ENCODING_FAILED`, `SPARSE_ENCODING_FAILED`, `ENCODING_MODEL_VERSION_MISSING`; `app/knowledge_access/application/encode_projection.py` livre les ports `DenseEncoder`, `SparseEncoder` et `ProjectionEncodingHandler`; `BuildFingerprint.extend_with_payload` intègre les versions et paramètres d'encodage dans l'empreinte régénérable.
- Garde-fous livrés: aucune version de modèle manquante n'est acceptée; aucun modèle dense ou sparse par défaut n'est inventé; un échec dense n'appelle pas sparse en compensation; un échec sparse refuse le résultat partiel dense; la trace d'encodage ne stocke pas le texte documentaire complet; l'orchestrateur KA ne dépend pas de Spark, `llm_gateway` ou `vLLM`.
- Traçabilité et gates: `REQ-M005-005` a été ajouté à `docs/traceability/matrix.md`; `scripts/test.ps1` enrôle les deux tests T-006; les preuves de gate sont alignées sur `58 exigence(s)` et `104 test(s)`.
- ADR: non requise; T-006 applique ADR-005, ADR-007, ADR-009 et DDD-ADR-004 sans changer leur sens ni déplacer les embeddings vers Spark.
- Validations GREEN: `tests/m005/validate_projection_encoding_acceptance.ps1`; `tests/m005/validate_projection_encoding_unit.ps1`; `scripts/validate_network_boundary.ps1`; `scripts/validate_traceability.ps1` (`58 exigence(s)`); `tests/m003/validate_m003_precondition_acceptance.ps1`; `tests/m004/validate_m004_traceability_acceptance.ps1`; `tests/governance/validate_m000_validation_commands_acceptance.ps1`; `git diff --check`; `scripts/lint.ps1` (`14 validation(s), 0 test(s)`); `scripts/test.ps1` (`14 validation(s), 104 test(s)`).
- Risques résiduels: T-006 livre les contrats de domaine et l'orchestrateur testable avec doubles déterministes; les adaptateurs d'embeddings réels, la persistance technique et la publication Qdrant restent portés par T-007 et les tâches suivantes.

## Clôture T-007

- Scénario BDD: Given une projection encodée avec tous ses chunks attendus; When KA publie l'index technique; Then la projection devient `SEARCHABLE` et publie `KnowledgeProjectionBecameSearchable` seulement si l'index contient tous les points versionnés et peut être reconstruit depuis les mêmes entrées.
- RED T-007 confirmé: `tests/m005/validate_qdrant_projection_acceptance.ps1`, `tests/m005/validate_qdrant_projection_unit.ps1` et `tests/m005/validate_knowledge_projection_events_acceptance.ps1` échouaient sur l'absence de `app.knowledge_access.adapters.in_memory_vector_index`, `app.knowledge_access.domain.projection_index` et `app.knowledge_access.application.projection_events`.
- Implémentation: `app/knowledge_access/domain/projection_index.py` livre le schéma d'index versionné, les points `VectorIndex`, l'identité de génération régénérable et l'erreur `INDEX_PARTIAL`; `app/knowledge_access/application/publish_projection_index.py` publie la génération, persiste les transitions `SEARCHABLE`, `FAILED`, `STALE` et `RETIRED`; `app/knowledge_access/application/projection_events.py` produit les événements KA via outbox M-002; `app/knowledge_access/adapters/in_memory_vector_index.py` livre l'adaptateur mémoire contractuel, car aucune dépendance `qdrant_client` n'existe dans le dépôt.
- Garde-fous livrés: aucune génération partielle n'est servie; aucun événement `KnowledgeProjectionBecameSearchable` n'est publié avant la publication complète; `KnowledgeProjectionFailed` nomme `INDEX_PARTIAL`; la suppression retire seulement l'index technique; le payload d'index refuse les champs de claim et identifiants Qdrant; RA et EG sont interdits d'import direct `qdrant_client` par `scripts/validate_architecture_boundaries.py`.
- Traçabilité et gates: `REQ-M005-006` a été ajouté à `docs/traceability/matrix.md`; `scripts/test.ps1` enrôle les trois tests T-007; les preuves de gate sont alignées sur `59 exigence(s)`, `103 test(s)` en précondition M-003 imbriquée et `107 test(s)` dans la gate globale.
- ADR: non requise; T-007 applique ADR-005, DDD-ADR-004 et DDD-ADR-008 sans changer leur sens. L'extension des suffixes d'événements `Searchable`, `Stale` et `Retired` matérialise les événements déjà spécifiés par M-005.
- Validations GREEN: `tests/m005/validate_qdrant_projection_acceptance.ps1`; `tests/m005/validate_qdrant_projection_unit.ps1`; `tests/m005/validate_knowledge_projection_events_acceptance.ps1`; `scripts/validate_architecture_boundaries.ps1`; `scripts/validate_traceability.ps1` (`59 exigence(s)`); `tests/m003/validate_m003_precondition_acceptance.ps1`; `tests/m004/validate_m004_traceability_acceptance.ps1`; `git diff --check`; `scripts/lint.ps1` (`14 validation(s), 0 test(s)`); `scripts/test.ps1` (`14 validation(s), 107 test(s)`).
- Risques résiduels: l'adaptateur livré reste un double contractuel mémoire; l'adaptateur Qdrant réel, la recherche hybride, la trace de recherche et l'API publique `POST /v1/search` restent portés par T-008 et T-009.

## Clôture T-008

- Scénario BDD: Given une projection `SEARCHABLE`; When `SearchKnowledge` exécute une recherche hybride; Then KA retourne des preuves candidates avec `SourceLocator` résolvable, `content_hash`, scores dense/sparse/rerank distincts, trace de fusion RRF et trace de recherche persistée.
- RED T-008 confirmé: `tests/m005/validate_hybrid_search_acceptance.ps1`, `tests/m005/validate_hybrid_search_unit.ps1` et `tests/m005/validate_search_trace_acceptance.ps1` échouaient sur l'absence de `app.knowledge_access.domain.search` et `app.knowledge_access.adapters.in_memory_hybrid_search`.
- Implémentation: `app/knowledge_access/domain/search.py` livre `SearchRequest`, `SearchResponse`, `RetrievalCandidate`, `SearchScoreBundle`, `HybridRetrievalPolicy`, `ParentContextExpansionPolicy`, `SearchTracePolicy` et `SearchTraceRecord`; `app/knowledge_access/application/search_knowledge.py` livre `SearchKnowledge` et le port `KnowledgeSearchPort`; `app/knowledge_access/adapters/in_memory_hybrid_search.py` livre les doubles contractuels de retrieval hybride, reranker et trace store.
- Garde-fous livrés: projection `STALE` ou non `SEARCHABLE` refusée sans fallback; filtre inconnu ou sans résultat utile refusé explicitement; fusion RRF déterministe; reranking exigé derrière un port; diversification par document après reranking; chaque candidat retourné résout son `SourceLocator`; aucun score n'est un verdict de vérité et KA ne retourne aucun claim EG ni conclusion RA.
- Traçabilité et gates: `REQ-M005-007` a été ajouté à `docs/traceability/matrix.md`; `scripts/test.ps1` enrôle les trois tests T-008; les preuves de gate sont alignées sur `60 exigence(s)`, `106 test(s)` en précondition M-003 imbriquée et `110 test(s)` dans la gate globale.
- ADR: non requise; T-008 applique ADR-005, ADR-006, DDD-ADR-003, DDD-ADR-004 et DDD-ADR-008 sans changer leur sens.
- Validations GREEN: `tests/m005/validate_hybrid_search_acceptance.ps1`; `tests/m005/validate_hybrid_search_unit.ps1`; `tests/m005/validate_search_trace_acceptance.ps1`; `tests/m004/validate_source_locator_resolution_acceptance.ps1`; `scripts/validate_traceability.ps1` (`60 exigence(s)`); `tests/m004/validate_m004_traceability_acceptance.ps1`; `tests/m003/validate_m003_precondition_acceptance.ps1`; `tests/governance/validate_m000_validation_commands_acceptance.ps1`; `git diff --check`; `scripts/lint.ps1` (`14 validation(s), 0 test(s)`); `scripts/test.ps1` (`14 validation(s), 110 test(s)`).
- Risques résiduels: T-008 livre le contrat applicatif et des doubles mémoire déterministes; l'adaptateur Qdrant réel et l'API publique `POST /v1/search` restent portés par T-009.

## Clôture T-009

- Scénario BDD: Given une projection actuelle est `SEARCHABLE`; When un client appelle `POST /v1/search` avec une requête publique valide; Then KA retourne des preuves candidates citées, scorées et traçables sans exposer Qdrant, embeddings, profils internes ni trace de fusion technique.
- RED T-009 confirmé: `tests/m005/validate_search_command_acceptance.ps1` et `tests/m005/validate_search_command_unit.ps1` échouaient sur `ModuleNotFoundError: No module named 'app.knowledge_access.adapters.search_http'` avant l'implémentation.
- Implémentation: `app/knowledge_access/adapters/search_http.py` livre `KnowledgeSearchHttpAdapter`, `SearchRequestDto`, `SearchResponseDto`, le routage strict `POST /v1/search`, le catalogue explicite de profils publics et le mapping des erreurs publiques; `app/knowledge_access/application/search_knowledge.py` expose `SearchProjectionNotFoundError`; `app/knowledge_access/adapters/__init__.py` exporte l'adaptateur public.
- Garde-fous livrés: corps vide, incomplet ou ambigu refusé; profil de recherche absent refusé sans profil par défaut; filtre inconnu refusé par `FILTER_NOT_SUPPORTED`; projection absente ou `STALE` refusée sans recherche vide; absence de `200` sur erreur métier; aucun endpoint RA ajouté; aucune confusion avec `POST /v1/documents/{document_id}/index`; la réponse publique ne contient pas les champs internes Qdrant, embeddings, profil de projection, `build_fingerprint`, génération d'index ou trace de fusion; l'adaptateur ne journalise pas de texte documentaire complet.
- Traçabilité et gates: `REQ-M005-008` a été ajouté à `docs/traceability/matrix.md`; `scripts/test.ps1` enrôle les deux tests T-009; les preuves de gate sont alignées sur `61 exigence(s)`, `108 test(s)` en précondition M-003 imbriquée et `112 test(s)` dans la gate globale.
- ADR: non requise; T-009 applique ADR-005, ADR-006, ADR-010, DDD-ADR-003 et DDD-ADR-004 sans changer leur sens.
- Validations GREEN: `tests/m005/validate_search_command_acceptance.ps1`; `tests/m005/validate_search_command_unit.ps1`; `tests/m005/validate_hybrid_search_acceptance.ps1`; `scripts/validate_traceability.ps1` (`61 exigence(s)`); `tests/m003/validate_m003_precondition_acceptance.ps1`; `tests/m004/validate_m004_traceability_acceptance.ps1`; `git diff --check`; `scripts/lint.ps1` (`14 validation(s), 0 test(s)`); `scripts/test.ps1` (`14 validation(s), 112 test(s)`).
- Risques résiduels: l'adaptateur reste un contrat HTTP local sans framework web; le branchement dans un serveur HTTP réel et le catalogue de profils de production restent des intégrations ultérieures. L'adaptateur Qdrant réel reste hors périmètre de T-009.

## Clôture T-010

- Scénario BDD: Given les comportements M-005 sont implémentés et testés; When les gates de clôture M-005 s'exécutent; Then chaque exigence M-005 est reliée à une preuve et les métriques Recall@k, MRR et nDCG initiales sont publiées comme mesures non définitives.
- RED T-010 confirmé: `tests/m005/validate_m005_traceability_unit.ps1` échouait sur `ModuleNotFoundError: No module named 'app.knowledge_access.application.traceability_metrics'`; `tests/m005/validate_m005_traceability_acceptance.ps1` révélait aussi que `scripts/validate_traceability.ps1` n'imposait pas encore toutes les exigences M-005 livrées.
- Implémentation: `app/knowledge_access/application/traceability_metrics.py` livre le calcul déterministe Recall@k, MRR et nDCG, le refus d'un jeu de questions absent, le signal `knowledge_search_initial_metrics_published` et le refus de payload d'audit contenant un passage documentaire complet.
- Preuves publiées: `tests/m005/fixtures/m005_initial_search_eval_fixture.json` identifie le corpus initial; `docs/governance/m005_initial_search_metrics.json` publie les mesures initiales non définitives, rattachées à la calibration M-012.
- Traçabilité et gates: `REQ-M005-009` a été ajouté à `docs/traceability/matrix.md`; `scripts/validate_traceability.ps1` impose maintenant `REQ-M005-003`, `REQ-M005-008` et `REQ-M005-009`; `scripts/test.ps1` enrôle les deux tests T-010; les compteurs de gates imbriquées M-003/M-004 et du self-test M-000 sont alignés.
- Garde-fous livrés: aucun seuil d'acceptation V1 avant M-012; aucune métrique sans corpus et questions; aucun texte documentaire complet dans les signaux d'audit; aucun claim vérifié ou conclusion RA dans les signaux KA.
- ADR: non requise; T-010 applique ADR-005, ADR-006, ADR-010, DDD-ADR-004 et DDD-ADR-008 sans changer leur sens.
- Validations GREEN: `tests/m005/validate_m005_traceability_acceptance.ps1`; `tests/m005/validate_m005_traceability_unit.ps1`; `scripts/validate_traceability.ps1` (`62 exigence(s)`); `tests/m003/validate_m003_precondition_acceptance.ps1`; `git diff --check`; `scripts/lint.ps1` (`14 validation(s), 0 test(s)`); `scripts/test.ps1` (`14 validation(s), 114 test(s)`).
- Risques résiduels: les métriques publiées restent une fixture initiale de clôture M-005, volontairement non représentative d'un seuil produit; la calibration des objectifs Recall/MRR/nDCG reste portée par M-012.
